# @package _global_
defaults:
  - oc_base
  - /prior: gauss_truncate
  - /sde: vp_10
  - /model@generative_ctrl: clipped_egnn_lj
  - /loss: time_reversal_lv
  - _self_

# Solver
solver:
  _target_: sde_sampler.solver.oc.Bridge

# Model
generative_ctrl:
  clip_model: 10.
