# @package _global_
defaults:
  - oc_base
  - /prior: gauss_truncate
  - /sde: vp_dw4
  - /model@generative_ctrl: lerp_dw4
  - /loss: time_reversal
  - _self_

# Solver
solver:
  _target_: sde_sampler.solver.oc.Bridge

# Model
generative_ctrl:
  clip_score: 1000.
  clip_model: 10.
