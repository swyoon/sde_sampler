# @package _global_
defaults:
  - oc_base
  - /prior: delta
  - /sde: bm_pis
  - /model@generative_ctrl: clipped_egnn_dw4
  - /loss: reference_sde
  - _self_

# Solver
solver:
  _target_: sde_sampler.solver.oc.PIS

# Model
generative_ctrl:
  clip_score: 1e4
  clip_model: 1e4